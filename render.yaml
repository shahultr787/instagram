services:
  - type: web
    name: video-converter
    env: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      npm install
    startCommand: node server.js
